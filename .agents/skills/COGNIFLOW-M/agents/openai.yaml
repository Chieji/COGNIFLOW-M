interface:
  display_name: "COGNIFLOW M"
  short_description: "Repo-specific patterns and workflows for COGNIFLOW-M"
  default_prompt: "Use the COGNIFLOW-M repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true